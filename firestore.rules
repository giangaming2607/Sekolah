rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { 
      // Allows local bypassed auth to read/write
      allow read, write: if true; 
    }
  }
}
